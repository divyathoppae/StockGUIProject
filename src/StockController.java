/**
 * The StockController interface defines
 * the method to start the stock management application.
 */
public interface StockController {
  /**
   * Starts the stock management application,
   * initiating user interaction and handling user inputs.
   */
  void start();
}